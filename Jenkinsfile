node() {
    stage('构建1') {
        //  注释
        checkout scm
        echo 'build'
    }
    stage('测试') {
        // 注释
        echo 'test1'
    }
    stage('部署') {
        // 注释
        echo 'deploy'
    }
    stage('验收') {
        // 注释
        echo 'deploy'
    }
}