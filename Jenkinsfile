node() {
    stage('构建') {
        //  注释
        checkout scm
        echo 'build'
    }
    stage('测试') {
        // 注释
        echo 'test'
    }
    stage('部署') {
        // 注释
        echo 'deploy'
    }
    stage('验收') {
        // 注释
        echo 'deploy'
    }
}